VITE_SUPABASE_URL=
#PUBLISHABLE_KEY等同于ANON_KEY二者取一即可
VITE_SUPABASE_PUBLISHABLE_KEY=  
VITE_SUPABASE_ANON_KEY=
# 以下两项只控制 AI 请求；创建用户和重置密码固定调用 admin-user-manager 边缘函数。
# 前端不需要管理员专用环境变量，也不得配置 SUPABASE_SERVICE_ROLE_KEY。
VITE_BACKEND_TARGET=flask
VITE_BACKEND_API_BASE_URL=http://localhost:5000
